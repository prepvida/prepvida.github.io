name: Deploy Supabase Edge Functions Automatically

on:
  push:
    branches:
      - main # Change this to 'master' if your main repository branch is named master

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code Repository
        uses: actions/checkout@v4

      - name: Setup Supabase CLI Tooling
        uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: Deploy Live Edge Functions
        run: |
          supabase functions deploy vapi-webhook --project-ref ${{ secrets.SUPABASE_PROJECT_ID }}
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
